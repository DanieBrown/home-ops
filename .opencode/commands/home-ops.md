---
description: Show the home-ops menu or route to a home-search command
---

Use the home-ops skill with the provided arguments:

$ARGUMENTS

Load the home-ops skill:
```
skill({ name: "home-ops" })
```